version: 0.1

environment_variables:
  plaintext:
    batchuser_pass: "aaaa"

phases:
  install:
    commands:
      - echo pre_build on `date`
      - echo `useradd testtest -p batchuser_pass`
      - echo `cat /etc/passwd`
      - echo `cp README.md /home/testtest/.`
      - echo `runuser -l testtest -c "touch text.txt"`
      - echo `runuser -l testtest -c "ls -l"`
      - echo `ls -l`
      - echo `runuser -l testtest -c "env1"`
      - echo `runuser -l testtest -c "env2"`      


  pre_build:
    commands:
      - echo Nothing to do in the pre_build phase...
  build:
    commands:
      - echo Test started on `date`

  post_build:
    commands:
      - echo Nothing to do in the pre_build phase...

